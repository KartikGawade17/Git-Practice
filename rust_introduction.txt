Hey! My name is rustin cohle partner of kartik this is my introduction file, from my local folder to github 
repo

important things to our project:
Data Normalization: It is the process of organizing the tables and columns in a relational database to reduce redundancy and preserve data integrity. It's commonly used to
like to eliminate the redundant data by decresing the tables size.
minimized the errors and anamolies.
simplify queries

fact = Data table and dimension = lookup tables
fact table: It contains numeric values or metrics used for summarization like (sales, orders, transaction, pageviews, etc)
dimension table: It contains descriptive data or attributes used for filtering or grouping (products, customers, dates, stores, etc)

PRIMARY KEY AND FOREIGN KEY
fact tables has foreign keys which are connected too dimension tables which has the primary key columns 


- RUSTINDARKNESS